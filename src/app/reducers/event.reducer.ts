import { ActionReducer, Action } from '@ngrx/store';
import { EventActions } from '../actions/event.action';

export const EventReducer: ActionReducer<any> = (state, {payload, type}: Action) =>  {

  switch (type) {
  }

}
