export class Organization {
    completed: string[];
    loading = false;
    success = true;
}

export const initialOrganization: Organization = {
    completed: [],
    loading: false,
    success: true,
};
